## Overview
This python script will fetch data from an ArcGIS REST API by filtering by a column that can be set by user. This script tests if the server allows pagination and if it is required based on number of features. 

